# 2026-06-10（晚二）— M2-3 完成：个人记忆库，"它马上学会了"第一次发生

## 本次做了什么

1. **src/brain/src/personal.rs**（新，~700 行含 6 单测）：PersonalLm（hashmap
   加权 stupid backoff，分母增量维护）+ PersonalStore——场景分桶（ADR-004
   阶梯：场景桶认词用桶，否则全局个人层）、个人词库（λ_lex bonus）、英文
   大小写习惯弧、词典外短语促升（2-6 字全中文 commit ≥2 次→个人词图弧）、
   拼音对齐（用用户实际键流消歧多音字）、追加式 commits.jsonl 持久化
2. **打分接入**：μ_g=0.65/μ_p=0.35/λ_lex=0.8（实验 001 获胜配置）固定为生产
   配置；lattice 加 PyPersonal/EnPersonal 弧（EN 对通用按 (end,token) 去重，
   镜像 lattice.py）；lm.rs 修了个人扩展 token id 越界（Vec 索引→get）
3. **插件 commit 通知**：Context::commit_notifier（fires 在 Clear 之前）→
   ipc-v0 commit；同管道响应必须读走保持消息流同步
4. **冷启动**：export_personal.py（jieba+文档衰减→personal.tsv 6361 行）；
   规格 lm-artifacts-v0.md 新增 §5 personal-artifacts-v0
5. **验收全过**：锚点（管道 + rime_console 双路）、重启回放、Python top-1
   一致 12/12（parity_personal.py）、bench 无回归（len20 中位 199µs）、37 单测

## 锚点实录

rime_console 两行输入：`suijitidu3`（选第 3 候选纠正）→ `suijitidu`（裸打）。
输出两行"随机梯度"。brain 侧学习 ~400µs，第二次 commit 自动把整短语促升为
个人词条。重启后分数逐位一致。

## 设计决定（偏离 STATUS 旧计划处）

- **jsonl 追加日志取代 rusqlite**：万级 commit 回放毫秒级；纯文本=用户可
  查看/删行（记忆可编辑原则）；零 C 依赖。规模需要时再上 DB，日志即迁移源
- **贪心分词 + 整短语促升**取代 jieba：贪心切不出的新词由短语促升兜住；
  系统性语料学习走 Python 导出路径

## 遗留（按优先级）

1. commit 学习不分"选字纠正/默认上屏"（应权重不同，协议加候选序号字段，M2-4 定）
2. scene 占位 `{}`，等 Weasel 给真实 app 身份（分桶逻辑已就绪）
3. commits.jsonl 无限增长，规模到了做 compact 快照
4. 管道抢注防护（继承）

## 下个 session 从哪继续

M2-4：部署进 Weasel 真实打字 vs 微软拼音（M2 验收门）。三件事：Weasel 安装
与 rime.dll 替换流程、真实 scene 信号（进程名）接入 commit/query、不完整
音节 composition 展示 + 兜底 schema。
