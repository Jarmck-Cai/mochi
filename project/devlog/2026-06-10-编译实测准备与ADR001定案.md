# 2026-06-10 — translator 编译实测准备完成，ADR-001 带条件定案

## 本次做了什么

后台 agent 完成编译实测的全部准备（experiments/003-translator-poc/），实际编译被本机环境卡住：

- **本机无任何 C++ 工具链**：无 VS/MSVC、LLVM、MinGW、CMake（Python 也是 Store 占位 stub）。按约束未做系统级安装，待用户同意后安装
- **可离线完成的全部就绪**：librime（master @ d71168e）+ 六个 deps 子模块浅克隆完毕；MochiTranslator 插件源码写齐并挂入 plugins/mochi/；最小 schema + 一键脚本 build-all.ps1（含 15ms 延迟模拟开关）。装完工具链一条命令约 30-60 分钟出结果
- **机制二次核对（最大发现）**：merged-plugin 比调研预期更干净——目录 GLOB → RIME_EXTRA_MODULES → kDefaultModules → 静态强引用全链路零阻塞，放目录即自动注册，**无需修改 librime 任何源文件**；官方 release CI 长期用同一机制合并三方插件
- **两个工程坑已记录**：env.bat.template 默认 ARCH=Win32（已改 x64）；install-boost.bat 依赖本机没有的 aria2c+7z（脚本已用 Invoke-WebRequest+tar 替代）
- **ADR-001 → Accepted（带条件）**：架构级风险已清零，编译实测顺延为 M2 第一项任务，翻车即回退

报告：docs/research/2026-06-10-translator-plugin-compile-test.md

## 下个 session 从哪继续

1. 用户同意后安装工具链：`winget install Microsoft.VisualStudio.2022.BuildTools` + `winget install Kitware.CMake`
2. 跑 experiments/003-translator-poc/build-all.ps1 → 完成 ADR-001 的实测条件 → M2 正式开工
3. 待用户中文语料 → M1 门禁补全
