# 2026-06-11（三）— M3 打磨包第一期：补全 + 模糊音 + 英文底座，反馈当天三连发

## 背景

用户全天真实使用（用 Mochi 打反馈消息本身），三轮反馈三轮闭环。本篇覆盖
第二轮（英文连写 + 纠正信号，commit aef200f）之后的第三轮。

## 第三轮反馈 → 实现

| 反馈 | 诊断 | 实现 |
|---|---|---|
| `congrat` 出"从rat" | 英文词表仅 1 万（congratulations 不在）且英文词全部 OOV 无排序 | wordfreq top-50k 换源 + zipf 频率按 λ=0.3 混入 unigram——英文词第一次有了彼此排序 |
| `separ` 想要补全 separate | 无补全机制 | **尾部补全弧**：每个位置的剩余串若是（音节/词 key/英文词/个人词）前缀，生成补全弧（惩罚 -2.0，区间扫描上限 300 防失控）。candidates 即 M5 幽灵文本的"阶段 1 候选窗预览" |
| `pinying` 不纠为拼音 | 无模糊音 | **模糊音**：z/zh c/ch s/sh in/ing en/eng an/ang，每词只生成"单音节替换"变体（防组合爆炸），惩罚 -1.5，preedit 显示规范读音（纠正本身） |
| `seperate` 拼写纠错 | 编辑距离容错 | 押后 M4（与置信度同批） |
| ghost 灰色内联 | Weasel UI/TSF | M5 主菜（本轮补全先以候选呈现） |

## 实测（真实产物）

- `congrat`→congratulations、`separ`→separate、`pinying`→拼音（屏营次选）、
  `shangxiaw`→上下文、`timew`→个人层补出 timewindow、`eeg` 次选 EEGLab
  （个人英文词补全）
- 通用层 `suijitidu` 补全出"随机梯度下降"（essay 完整术语）；个人层叠加后
  "随机梯度"仍稳居第一
- 回归全保持；44 单测全过
- 加载：低功耗状态实测 24.6s，按当时 ~12x 机器减速归一化 ≈2s（阈值内；
  机器恢复后需复测确认——本机功耗状态对绝对值影响 10x+，所有性能对比
  必须同会话 A/B）

## 设计要点

- 补全弧 `completed` 标记 + 惩罚叠加在原弧型先验上；纯补全位置仍补 literal
  fallback——原样候选（我要c）永远活着
- 补全索引只收录规范 key（fuzzy 变体不参与补全：两层猜测不叠加）
- 个人词补全走小匹配器线性扫（个人词少而金贵，前缀下限放宽到 2）

## 遗留

- 形式化 A/B 这轮没跑（essay 代产物没备份就重建了）——下次底座变更前先备份
- 加载时间正常功耗下复测；若超 3s 考虑二进制缓存（artifacts.rs 留过此选项）
- wordfreq 50k 中的低频词可能给英文垃圾解析提供弧——观察真实使用，必要时
  按 rank 加先验斜坡

## 下个 session 从哪继续

真实使用观察这三件套的体感（特别是补全的噪声水平）；brain 自启；M2 正式
验收（vs 微软拼音）仍在进行。中期：现代大语料重训（M3 二期）。
