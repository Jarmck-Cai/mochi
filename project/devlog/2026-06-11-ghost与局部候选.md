# 2026-06-11（四）— Ghost 全链路 + 局部候选：一天的第五、六轮闭环

## 本次做了什么

1. **Ghost 阶段 0→1 全部落地**：文字空间 ▸ 标记（按读音覆盖度逐字判定）、
   preedit ‥ 边界、fork weasel 0.17.4（4 文件 102 行：GHOST 属性 +
   ▸ comment 解析 + DWrite SetDrawingEffect 区间灰绘）并部署。用户反馈
   "没看到灰色"→ 诊断为 aqua 配色未定义 comment 色（灰被画成正文同色），
   weasel.custom.yaml 补 0x999999 后点亮
2. **局部候选（长句就地修复）**：decoder 发射各前缀位置最优候选（cap 6
   长在前），垃圾双滤（路径含原始字母 = 中音节切点；按键均摊分数差于
   整句最优 0.5 = 垃圾解析如"常用词很zh on"——第一版只滤 fallback 不够，
   英文短词弧也会制造垃圾切点）；Candidate 加 len（serde default 兼容）；
   插件按 len 设候选跨度。**rime 部分选择原生兼容**：e2e 实证
   changyongcihenzhongyao → 8 选「常用词」→ 剩余 henzhongyao 继续组字
   → 1 选「很重要」→ 上屏「常用词很重要」
3. **安全闸**：新 brain + 旧 rime.dll 窗口期，前缀候选会以整段跨度上屏
   吃掉剩余拼音——加 --no-prefix 旗标，服务端暂以此运行

## 卡点（下次开工第一件事）

**rime.dll 部署等 UAC**：管理员跑 `experiments\004-weasel-deploy\deploy-weasel.ps1`
→ 重启 brain 去掉 `--no-prefix`。两步完成后局部候选在真实打字可用。

## 经验沉淀

- weasel 构建链：BuildTools 要补 ATL；.rc 的 afxres.h→winres.h 免装 MFC；
  rime 头/库放 weasel\librime\include 与 lib64+lib；boost 复用 librime
  deps 源码树；Win32 变体 Server/Deployer 失败可忽略
- PS 5.1 提权脚本：native exe 的 stderr 重定向 + EAP Stop 会当场抛异常
  ——提权脚本必须 try/catch 全包 + 错误也写日志，否则静默死无从诊断
- 跨进程灰度发布意识：协议加字段（len）时，旧消费端的行为要先推演
  ——这次差点让用户的选择吃掉半句话
- 配色方案可能不定义某些颜色项——借用样式色时要查激活方案的实际值

## 下个 session 从哪继续

① UAC 部署 rime.dll + brain 去旗标重启（见上）；② 真实打字验收局部候选
与灰色 ghost；③ 观察项：30 候选的序列化开销（慢功耗态 brain_us 6.3ms，
正常态应 <1ms，必要时砍 topn 或延迟物化）；④ M2 对比微软拼音继续
