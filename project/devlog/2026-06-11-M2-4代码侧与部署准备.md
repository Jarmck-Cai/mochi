# 2026-06-11 — M2-4 代码侧完成：尾部修复 + 真实场景信号 + 部署套件

## 本次做了什么

1. **"我压oc"根因与修复**：preedit 暴露真相——尾巴 "oc" 被通用英文词表的
   垃圾短词当英文弧（-3）吃掉，打败了"我要+c"（fallback -12）。修法：词图
   构建后回扫，纯 fallback 后缀=未打完的音节，标 FallbackTail（-1.5）。
   `woyaoc`→我要c、`wuyaoc`→五要c；完整拼音/混打不受影响。39 单测过
2. **SceneProbe（插件）**：前台窗口→进程名，按 HWND 缓存（热路径 ~µs）；
   query/commit 的 scene.app 接真实值；brain 日志加 app= 字段。headless
   会话验证了优雅降级（hwnd=0→`{}`→全局记忆）
3. **experiments/004-weasel-deploy**：mochi schema v0.2（+标点/Shift
   commit_code 切换）、default.custom.yaml（与明月并存，F4 兜底）、
   deploy-weasel.ps1（停服→换 dll 自动备份→部署 schema→重启；-Restore
   一键回滚）、README（部署步骤 + 风险表 + M2 验收协议：20 句混打对比 +
   3 天真实使用）

## 卡点

Weasel 系统级安装需要 UAC 提权，后台会话无法代点（winget 卡死一次、提权
拉起一次均未获确认）。**继续只差三条命令**（见 STATUS"进行中"），都备好了。

## 经验沉淀

- 非交互会话 `GetForegroundWindow()` 返回 0——场景探测必须容忍空值（已设计如此）
- winget 在无人值守环境会无限等 UAC：先 `IsInRole(Administrator)` 探权限，
  需要提权的安装直接交给用户或用 `-Verb RunAs`（仍需人点）

## 下个 session 从哪继续

用户侧三步（管理员装 Weasel → 跑 deploy-weasel.ps1 → 起 brain），然后：
记事本冒烟（候选/学习/app= 日志）→ 微信实测场景分桶 → 按协议对比微软拼音。
观察项：真实 app 下 elapsed_us 分布、commit 学习信号质量（要不要分级）。
