# 2026-06-10 — M2-1/M2-2 完成：IPC 链路 + 真实解码进 brain

## 本次做了什么

1. **ADR-003 定案（Rust）** + IPC 协议 v0（docs/specs/ipc-v0.md）+ lm-artifacts-v0 契约与导出器
2. **M2-1 E2E**：src/brain（Rust）+ src/ime-plugin（C++）echo 链路实测通过；稳态 e2e 中位 0.26-1.2ms；降级 23µs 检出、重启自愈
3. **M2-2 解码**：实验 001 算法移植进 brain（artifacts/interner/lattice/lm/decoder/engine 六模块，26 单测）。rime_console 实测 110 键 0 掉线全对；解码中位 ≤200µs（预算 4%）；Python top-1 一致 10/10
4. **两个超时真 bug 同根连修**：① deadline 用 GetTickCount64 计算被 10-16ms 量化（主会话修，换 QPC）；② WaitForSingleObject 的超时参数本身也被节拍量化、可提前近一拍返回（M2-2 agent 修，QPC 重等待循环；压测 3030 键 133 次误判→0）。**教训沉淀：Windows 上贴毫秒级预算的一切等待，deadline 与 wait 都必须 QPC 兜底**
5. 部署坑沉淀：librime install 步骤覆盖 bin 的 default.yaml（重建后必须重部署 rime-data + 清 bin\build）

## 验收锚点（M2-3 用）

`suijitidu` 当前出"随即梯度"（通用 LM 错，与实验 001 赢例对应）——个人记忆库接入并纠正一次后应出"随机梯度"。这是"它马上学会了"的第一个可演示用例。

## 遗留（按优先级）

1. 插件侧 commit 通知未发（M2-3 即时学习的输入，e2e 报告遗留 3）
2. 管道抢注防护（FIRST_PIPE_INSTANCE + ACL，产品化前）
3. 不完整音节尾部候选展示（"我压oc"，M2-4 composition 议题）
4. quality 字段语义未定义（现填 log10 模型分）

## 下个 session 从哪继续

M2-3 个人记忆库（PersonalLayer trait 已留接口）：场景分桶 + 衰减计数 + commit 闭环 + 文档导入。验收 = 锚点用例 + 实验 001 指标在 Rust 侧复算一致。
