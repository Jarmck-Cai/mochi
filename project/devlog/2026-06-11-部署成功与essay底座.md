# 2026-06-11（二）— 部署成功，首日真实使用，反馈当天闭环

## 本次做了什么

1. **部署落地**：UAC 通过后 Weasel 0.17.4 安装、rime.dll 替换（hash 验证、
   原版备份）、mochi schema 部署全部成功。一度误判"mochi 模块没加载"——
   实为 glog 缓冲未刷盘，优雅重启服务后日志实证 `registering component:
   mochi_translator`。用户开始真实打字（甚至用 Mochi 打出了反馈消息本身），
   场景信号（notepad.exe / windowsterminal.exe）与即时学习全链路工作
2. **首日反馈**（用户）：基本可用、语料覆盖处（EEG）体验好；但候选只有
   5 个无翻页、"有些常用词"首位是"有些常用此"——提出"用常用语库作底座"
3. **诊断**：三数据点——"常用词"不在 rime 词典；通用 LM 是 1998 新闻语料
   （此:词 = 19:1 书面语偏置，"常用→词" bigram 为零）；正确答案其实排 #4
   但 5 候选无翻页体感差
4. **当天闭环两项**：
   - 候选 5→24（brain topn）+ page_size 9 + -/= 翻页（schema v0.3；
     逗号句号不绑翻页——会和标点上屏冲突）
   - **essay 八股文进通用层**：opencc 繁→简 + pypinyin 注音，30.8 万复合词
     进词典（37.3 万词条），全量词频按 λ=1.0 混入 unigram（50/50）。
     "有些常用词"→#1；80 句真实语料 A/B：通用层 top-1 42.5%→46.2%
     （5 胜 2 负，胜例如"脑卒中康复训练"）
5. **性能回填**：词典 5.7 倍后加载 11.8s——前缀集 450 万次 Box<str> 分配
   是热点，改存 FxHash u64（碰撞只多一次 map miss）→ **1.15s**；查询延迟
   无回归（len20 中位 189µs）

## 真实使用观察（记入待办权衡）

- 用户引用错例时上屏了"有些常用此"——commit 学习如实记下。当前 essay 底座
  + 一次正确选择的分数足以压制；但"信号分级"（选字纠正 > 默认上屏）的需求
  被真实案例坐实了
- 机器重启后 brain 不在——"自动拉起"升为体验前置项

## 下个 session 从哪继续

M2 正式验收进行中：用户按 experiments/004 README 协议真实使用对比微软拼音
（≥3 天）。开发侧下一项：brain 自启（注册开机启动或插件拉起）；之后按反馈
决定 commit 信号分级方案。中期：现代大语料重训 trigram（M3）。
