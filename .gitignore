# Build artifacts
build/
out/
target/
src/brain/*.log
*.obj
*.exe
*.dll
*.pdb

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Models & large data (track via scripts, not git)
*.onnx
*.gguf
*.bin
*.safetensors
experiments/**/data/
experiments/**/output/

# Offline LM artifacts (rebuild via experiments/001 export_artifacts.py);
# keep meta.json for provenance
artifacts/**
!artifacts/v0/
!artifacts/v0/meta.json

# Personal typing data — NEVER commit (privacy)
**/user_data/
**/*.typing.log

# UIA probe raw captures — contain real on-screen content (chat messages etc.)
experiments/002-uia-probe/*-output.txt

# Shallow-cloned third-party source trees (rebuild via experiments/003 README)
experiments/003-translator-poc/librime/
experiments/weasel-fork-probe/

# Release staging (zips uploaded to GitHub Releases, not git)
dist/

# Local env & editor
.env
.vscode/
.idea/
*.local.json

# Logs
logs/
*.log
